#include <stdio.h>
#include <math.h>

// scanf va printf operatoridan foydalanganda
// int uchun %d
// double %lf
// float uchun %f
// sqrt sonning ildizini topish uchun ishlatiladi
// f=ildizda (x^4+1-y^2)/a^5+ildizda(y^ldizda(x+1))





int main() {

 double t=2, l=3, R;

 R = 3*pow(t, 2)+3*pow(l, 5)+4.9;

 printf("natija: %lf\n", R );


 return 0;
}
