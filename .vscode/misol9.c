#include <stdio.h>
#include <math.h>

// scanf va printf operatoridan foydalanganda
// int uchun %d
// double %lf
// float uchun %f
// sqrt sonning ildizini topish uchun ishlatiladi





int main() {

  double a=2, r=1, b=4, y=5, x=1, k=1, D;

  D=((1/pow(k, a*r*x))-a*sqrt(6)-cos(3*a*b))/pow(sin(a*asin(x)+log(y)), 2);

  printf("natija: %lf\n", D );


 return 0;
}
