#include <stdio.h>
#include <math.h>


int main(){

    int A=-12, B=10, C=6, Natija=0;

    if(A>0) Natija++;
    if(B>0) Natija++;
    if(C>0) Natija++;

    printf("Natija: %d", Natija);

    return 0;
}